package ca.waterloo.dsg.graphflow.query.utils;

/**
 * Common interface representing a structured query. It is used in {@code GraphflowVisitor} to
 * return parsed results.
 */
public interface AbstractStructuredQuery {}
